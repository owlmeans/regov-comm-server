{
  "name": "@owlmeans/regov-comm-server",
  "version": "0.1.6",
  "description": "Node.js Re:gov server wallet agent for SSI",
  "main": "build/index.js",
  "author": "Igor Tkachenko",
  "license": "Apache-2.0",
  "private": true,
  "dependencies": {
    "@owlmeans/regov-comm": "^0.2.12",
    "@owlmeans/regov-ssi-core": "^0.6.12",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.30",
    "nodemon": "^2.0.16",
    "ts-node": "^10.7.0",
    "typescript": "^4.3.5"
  },
  "scripts": {
    "dev": "nodemon --watch './**/*.ts' --watch '../src/**/*' --exec 'ts-node' src/index.ts",
    "build": "tsc -b -f",
    "start": "node --openssl-legacy-provider ./build/index.js"
  }
}
